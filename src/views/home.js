import React, { Fragment } from "react";

import { Hero, FormContent } from "../components";

const Home = () => (
  <Fragment>
    <Hero />
    <hr />
    <FormContent />
  </Fragment>
);

export default Home;
